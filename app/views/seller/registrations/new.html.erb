<%= render '/shared/navbar' %>

<section class="section">
  <h1>Apply as Seller</h1>
  In order to apply as a seller, you would need to submit to us the following:
  <br/>
  * Name
  <br/>
  * Breeder Supplier Number
  <br/>
  * Photo/scan of your Breeder ID that matches your name and breeder prefix id
  <br/>
  * The email associated with this user (and where we'll send updates to)
</p>
  <div class="container">
    <div class="card">
        <div class="card-content">
          <div class="content is-small is-narrow">
            <h2>Apply as Seller</h2>
            <%= form_for(@user, url: seller_registrations_path, html: { method: :post }) do |f| %>

            <div class="field">
              <%= f.label :name %><br />
              <%= f.text_field :name, autofocus: true %>
            </div>

            <div class="field">
              <%= f.label :breeder_supply_number, 'Breeder Supply Number' %><br />
              <%= f.text_field :breeder_supply_number, maxlength:10, placeholder:'e.g. 1234567890', autofocus: true %>
            </div>

            <div class="field">
              <%= f.label :breeder_id_picture %><br />
              <%= f.file_field :breeder_id_picture, accept:'image/png,image/gif,image/jpeg,image/jpg' %>
            </div>

            <div class="field">
              <%= f.label :email %><br />
              <%= f.email_field :email, autofocus: true, autocomplete: "email" %>
            </div>
        </div>

        <div class="actions">
            <%= f.submit "Apply as Seller" %>
        </div>
      </div>
    <% end %>
  </div>

</section>
